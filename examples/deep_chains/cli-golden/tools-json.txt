# megascope tools --format json -> exit 0
{"count":24,"tools":[{"cli":true,"description":"Look up metadata for a function by qualified name. Returns file, line, class membership, and virtual status. If several functions share the name (overloads, template specializations), returns {ambiguous:true, candidates:[...]} \u2014 re-query with the 'usr' of the intended candidate.","inputSchema":{"properties":{"filter":{"description":"Literal substring matched against candidate usr, qualified name, and file to narrow an ambiguous name; resolves when exactly one candidate survives. NOT type-aware: use names that appear literally (e.g. a template argument's class name), not guessed signatures.","type":"string"},"name":{"description":"Qualified function name (e.g. 'MyClass::process'). Provide 'name' or 'usr' (usr wins when both are present).","type":"string"},"site":{"description":"Call site 'file:line:col' (as spelled in the compile command). Resolves the identity to the exact overload/template instantiation called at that site \u2014 the most precise disambiguator when you are looking at a specific call.","type":"string"},"usr":{"description":"Exact function USR (from search_functions results or a prior disambiguation response). Bypasses name resolution entirely \u2014 use it to pick one overload/specialization when a name is ambiguous.","type":"string"}},"type":"object"},"name":"lookup_function","needs":["graph"],"records":"","verb":"lookup-function"},{"cli":true,"description":"List all functions called by a given function. Supports filtering by edge kind and confidence level. An ambiguous name returns {ambiguous:true, candidates:[...]} \u2014 re-query with 'usr'.","inputSchema":{"properties":{"edge_kinds":{"description":"Filter by edge kind: DirectCall, VirtualDispatch, FunctionPointer, ConstructorCall, DestructorCall, OperatorCall, TemplateInstantiation, LambdaCall, ThreadEntry","items":{"type":"string"},"type":"array"},"execution_contexts":{"description":"Filter by execution context: Synchronous, ThreadSpawn, AsyncTask, PackagedTask, Invoke. Default: all contexts.","items":{"type":"string"},"type":"array"},"filter":{"description":"Literal substring matched against candidate usr, qualified name, and file to narrow an ambiguous name; resolves when exactly one candidate survives. NOT type-aware: use names that appear literally (e.g. a template argument's class name), not guessed signatures.","type":"string"},"include_confidences":{"description":"Explicit set of confidence tiers to include (e.g. [\"Plausible\"] returns only Plausible edges). Overrides min_confidence.","items":{"type":"string"},"type":"array"},"min_confidence":{"description":"Inclusive minimum confidence tier: Proven, Plausible, or Unknown (default: Unknown). Plausible includes both Plausible and Proven edges. Use include_confidences to select exact tiers.","type":"string"},"name":{"description":"Qualified name of the caller function. Provide 'name' or 'usr' (usr wins when both are present).","type":"string"},"site":{"description":"Call site 'file:line:col' (as spelled in the compile command). Resolves the identity to the exact overload/template instantiation called at that site \u2014 the most precise disambiguator when you are looking at a specific call.","type":"string"},"usr":{"description":"Exact function USR of the caller. Bypasses name resolution \u2014 use it to pick one overload/specialization when the name is ambiguous.","type":"string"}},"type":"object"},"name":"get_callees","needs":["graph"],"records":"callees","verb":"get-callees"},{"cli":true,"description":"List all functions that call a given function. Supports filtering by edge kind and confidence level. An ambiguous name returns {ambiguous:true, candidates:[...]} \u2014 re-query with 'usr'.","inputSchema":{"properties":{"edge_kinds":{"description":"Filter by edge kind: DirectCall, VirtualDispatch, FunctionPointer, ConstructorCall, DestructorCall, OperatorCall, TemplateInstantiation, LambdaCall, ThreadEntry","items":{"type":"string"},"type":"array"},"execution_contexts":{"description":"Filter by execution context: Synchronous, ThreadSpawn, AsyncTask, PackagedTask, Invoke. Default: all contexts.","items":{"type":"string"},"type":"array"},"filter":{"description":"Literal substring matched against candidate usr, qualified name, and file to narrow an ambiguous name; resolves when exactly one candidate survives. NOT type-aware: use names that appear literally (e.g. a template argument's class name), not guessed signatures.","type":"string"},"include_confidences":{"description":"Explicit set of confidence tiers to include. Overrides min_confidence.","items":{"type":"string"},"type":"array"},"min_confidence":{"description":"Inclusive minimum confidence tier: Proven, Plausible, or Unknown (default: Unknown). Plausible includes both Plausible and Proven edges. Use include_confidences to select exact tiers.","type":"string"},"name":{"description":"Qualified name of the callee function. Provide 'name' or 'usr' (usr wins when both are present).","type":"string"},"site":{"description":"Call site 'file:line:col' (as spelled in the compile command). Resolves the identity to the exact overload/template instantiation called at that site \u2014 the most precise disambiguator when you are looking at a specific call.","type":"string"},"usr":{"description":"Exact function USR of the callee. Bypasses name resolution \u2014 use it to pick one overload/specialization when the name is ambiguous.","type":"string"}},"type":"object"},"name":"get_callers","needs":["graph"],"records":"callers","verb":"get-callers"},{"cli":true,"description":"Find call chains from a source function (or entry points) to a target function. Each path is an array of hop objects with {from, to, kind, confidence, callSite, executionContext?}. executionContext is only emitted on ThreadEntry hops and other non-Synchronous edges. An ambiguous 'from' or 'to' name returns {ambiguous:true, candidates:[...]} \u2014 re-query with the *_usr parameter.","inputSchema":{"properties":{"edge_kinds":{"description":"Prune hops whose edge kind is not in this set.","items":{"type":"string"},"type":"array"},"from":{"description":"Source function qualified name (omit to use entry points)","type":"string"},"from_filter":{"description":"Literal substring matched against candidate usr, qualified name, and file to narrow an ambiguous name; resolves when exactly one candidate survives. NOT type-aware: use names that appear literally (e.g. a template argument's class name), not guessed signatures.","type":"string"},"from_site":{"description":"Call site 'file:line:col' (as spelled in the compile command). Resolves the identity to the exact overload/template instantiation called at that site \u2014 the most precise disambiguator when you are looking at a specific call.","type":"string"},"from_usr":{"description":"Exact USR of the source function. Bypasses name resolution for 'from' when the name is ambiguous.","type":"string"},"include_confidences":{"description":"Explicit set of confidence tiers allowed at every hop. Overrides min_confidence.","items":{"type":"string"},"type":"array"},"max_depth":{"description":"Maximum number of edges in a chain, i.e. node count minus one (default: 20)","type":"integer"},"max_fan_in":{"description":"Skip expanding the ancestry of functions with more stored callers than this (high-fan-in hubs like loggers); skipped hubs are listed in the response. 0 disables the cutoff (default: 1000).","type":"integer"},"max_paths":{"description":"Maximum number of paths to return (default: 10)","type":"integer"},"min_confidence":{"description":"Inclusive minimum confidence tier applied to every hop on the path (default: Unknown).","type":"string"},"to":{"description":"Target function qualified name. Provide 'to' or 'to_usr' (to_usr wins when both are present).","type":"string"},"to_filter":{"description":"Literal substring matched against candidate usr, qualified name, and file to narrow an ambiguous name; resolves when exactly one candidate survives. NOT type-aware: use names that appear literally (e.g. a template argument's class name), not guessed signatures.","type":"string"},"to_site":{"description":"Call site 'file:line:col' (as spelled in the compile command). Resolves the identity to the exact overload/template instantiation called at that site \u2014 the most precise disambiguator when you are looking at a specific call.","type":"string"},"to_usr":{"description":"Exact USR of the target function. Bypasses name resolution for 'to' when the name is ambiguous.","type":"string"}},"type":"object"},"name":"find_call_chain","needs":["graph"],"records":"paths","verb":"find-call-chain"},{"cli":true,"description":"Find functions by name substring when the exact qualified name is unknown. Returns candidates ranked by match quality (exact unqualified name, then prefix, then substring). Use this before lookup_function / get_callers when unsure of the precise name.","inputSchema":{"properties":{"limit":{"description":"Maximum matches to return (default: 25)","type":"integer"},"query":{"description":"Case-insensitive substring to match against qualified function names (e.g. 'execute' or 'TransformPipeline').","type":"string"}},"required":["query"],"type":"object"},"name":"search_functions","needs":["graph"],"records":"matches","verb":"search-functions"},{"cli":true,"description":"Query class inheritance relationships and virtual method overrides. Shows derived classes and optionally which methods are overridden in each.","inputSchema":{"properties":{"class_name":{"description":"Qualified class name","type":"string"},"include_overrides":{"description":"Include virtual method override info (default: false)","type":"boolean"},"include_transitive":{"description":"Include all descendants, not just direct (default: false)","type":"boolean"}},"required":["class_name"],"type":"object"},"name":"get_class_hierarchy","needs":["graph"],"records":"derivedClasses","verb":"get-class-hierarchy"},{"cli":true,"description":"List the configured entry-point functions with their file/line when resolved in the call graph. Useful for orientation before calling find_call_chain or analyze_dead_code.","inputSchema":{"properties":{},"type":"object"},"name":"list_entry_points","needs":["graph"],"records":"entryPoints","verb":"list-entry-points"},{"cli":true,"description":"Return aggregate statistics about the call graph: node and edge counts, call-site count, entry-point count, top-5 fan-out callers and callees, and histograms by confidence and edge kind.","inputSchema":{"properties":{},"type":"object"},"name":"graph_summary","needs":["graph"],"records":"","verb":"graph-summary"},{"cli":true,"description":"List every callback registration or invocation site grouped by target. Covers FunctionPointer and LambdaCall edges, including synthetic lambda targets named 'lambda#file:line:col#enclosing'. Returns {target, siteCount, sites:[{caller, callSite, kind, confidence, indirectionDepth?, executionContext?}]}.","inputSchema":{"properties":{"target_prefix":{"description":"Optional qualified-name prefix; only targets whose name starts with this prefix are returned.","type":"string"}},"type":"object"},"name":"list_callback_sites","needs":["graph"],"records":"targets","verb":"list-callback-sites"},{"cli":true,"description":"List every ThreadEntry edge: functions (including synthetic lambda targets) that are handed to std::thread, std::jthread, std::async, std::packaged_task, std::invoke, or std::bind. Returns {count, entries:[{spawner, target, executionContext, callSite, confidence}]}.","inputSchema":{"properties":{"execution_contexts":{"description":"Filter by execution context: ThreadSpawn, AsyncTask, PackagedTask, Invoke. Default: all ThreadEntry contexts.","items":{"type":"string"},"type":"array"}},"type":"object"},"name":"list_concurrency_entry_points","needs":["graph"],"records":"entries","verb":"list-concurrency-entry-points"},{"cli":true,"description":"Determine whether a function is protected by try/catch on its call paths from entry points. Reports always, sometimes, or never caught. An ambiguous name returns {ambiguous:true, candidates:[...]} \u2014 re-query with 'usr'.","inputSchema":{"properties":{"entry_points":{"description":"Entry point function names (default: configured entry points)","items":{"type":"string"},"type":"array"},"exception_type":{"description":"Exception type to check (e.g. 'std::runtime_error')","type":"string"},"filter":{"description":"Literal substring matched against candidate usr, qualified name, and file to narrow an ambiguous name; resolves when exactly one candidate survives. NOT type-aware: use names that appear literally (e.g. a template argument's class name), not guessed signatures.","type":"string"},"function":{"description":"Target function qualified name. Provide 'function' or 'usr' (usr wins when both are present).","type":"string"},"site":{"description":"Call site 'file:line:col' (as spelled in the compile command). Resolves the identity to the exact overload/template instantiation called at that site \u2014 the most precise disambiguator when you are looking at a specific call.","type":"string"},"usr":{"description":"Exact USR of the target function. Bypasses name resolution \u2014 use it to pick one overload/specialization when the name is ambiguous.","type":"string"}},"type":"object"},"name":"query_exception_safety","needs":["graph","control_flow"],"records":"","verb":"query-exception-safety"},{"cli":true,"description":"Get exception handling and guard context at a specific call site location (file:line:col). Shows enclosing try/catch scopes and conditional guards. A spelling shared by several call sites (macro expansion) returns {ambiguous:true, candidates:[...]} \u2014 re-query with 'caller'.","inputSchema":{"properties":{"call_site":{"description":"Call site location formatted as 'file:line:col'. The file path must match the compilation database canonicalization (typically an absolute path). Returns isError if the site is not indexed.","type":"string"},"caller":{"description":"Optional enclosing function (qualified name or USR). Needed when several call sites share one spelling (a macro expanded in different functions); without it such a spelling returns {ambiguous:true, candidates:[...]} listing the callers.","type":"string"}},"required":["call_site"],"type":"object"},"name":"query_call_site_context","needs":["graph","control_flow"],"records":"","verb":"query-call-site-context"},{"cli":true,"description":"List RAII-capable locals (non-trivial-destructor) live at a call site. Each entry is {typeName, varName, kind, declLocation}, kind in {lock, smart_ptr, other}. Use `kinds` to narrow the response (e.g. [\"lock\"] for concurrency audits). A spelling shared by several call sites (macro expansion) returns {ambiguous:true, candidates:[...]} \u2014 re-query with 'caller'.","inputSchema":{"properties":{"call_site":{"description":"Call site location formatted as 'file:line:col'. Must match the compilation database canonicalization.","type":"string"},"caller":{"description":"Optional enclosing function (qualified name or USR). Needed when several call sites share one spelling (a macro expanded in different functions); without it such a spelling returns {ambiguous:true, candidates:[...]} listing the callers.","type":"string"},"kinds":{"description":"Filter by kind: lock, smart_ptr, other. Default: all kinds.","items":{"type":"string"},"type":"array"}},"required":["call_site"],"type":"object"},"name":"query_raii_scopes_at_callsite","needs":["graph","control_flow"],"records":"locals","verb":"query-raii-scopes-at-callsite"},{"cli":true,"description":"If a function throws the given exception type, is it caught before unwinding to an entry point? Reports the protection verdict plus every call path with its try/catch scopes, guards, and where the throw would be caught. An ambiguous name returns {ambiguous:true, candidates:[...]} \u2014 re-query with 'usr'.","inputSchema":{"properties":{"entry_points":{"description":"Entry point function names (default: configured entry points)","items":{"type":"string"},"type":"array"},"exception_type":{"description":"Exception type the function throws (e.g. 'std::runtime_error'); empty matches any handler","type":"string"},"filter":{"description":"Literal substring matched against candidate usr, qualified name, and file to narrow an ambiguous name; resolves when exactly one candidate survives. NOT type-aware: use names that appear literally (e.g. a template argument's class name), not guessed signatures.","type":"string"},"function":{"description":"Target function qualified name. Provide 'function' or 'usr' (usr wins when both are present).","type":"string"},"site":{"description":"Call site 'file:line:col' (as spelled in the compile command). Resolves the identity to the exact overload/template instantiation called at that site \u2014 the most precise disambiguator when you are looking at a specific call.","type":"string"},"usr":{"description":"Exact USR of the target function. Bypasses name resolution \u2014 use it to pick one overload/specialization when the name is ambiguous.","type":"string"}},"type":"object"},"name":"query_throw_propagation","needs":["graph","control_flow"],"records":"paths","verb":"query-throw-propagation"},{"cli":true,"description":"Enumerate the call paths from the entry points to a function, each with the try/catch scopes and conditional guards along it. The exception-context counterpart of find_call_chain. An ambiguous name returns {ambiguous:true, candidates:[...]} \u2014 re-query with 'usr'.","inputSchema":{"properties":{"entry_points":{"description":"Entry point function names (default: configured entry points)","items":{"type":"string"},"type":"array"},"filter":{"description":"Literal substring matched against candidate usr, qualified name, and file to narrow an ambiguous name; resolves when exactly one candidate survives. NOT type-aware: use names that appear literally (e.g. a template argument's class name), not guessed signatures.","type":"string"},"function":{"description":"Target function qualified name. Provide 'function' or 'usr' (usr wins when both are present).","type":"string"},"max_paths":{"description":"Maximum number of paths to enumerate (default 100)","type":"integer"},"site":{"description":"Call site 'file:line:col' (as spelled in the compile command). Resolves the identity to the exact overload/template instantiation called at that site \u2014 the most precise disambiguator when you are looking at a specific call.","type":"string"},"usr":{"description":"Exact USR of the target function. Bypasses name resolution \u2014 use it to pick one overload/specialization when the name is ambiguous.","type":"string"}},"type":"object"},"name":"query_all_path_contexts","needs":["graph","control_flow"],"records":"paths","verb":"query-all-path-contexts"},{"cli":true,"description":"For each call path into a function, the nearest enclosing try/catch walking up the callers: the handler scope, how many frames up it sits, and the path segment between. Empty when nothing on any path catches. An ambiguous name returns {ambiguous:true, candidates:[...]} \u2014 re-query with 'usr'.","inputSchema":{"properties":{"filter":{"description":"Literal substring matched against candidate usr, qualified name, and file to narrow an ambiguous name; resolves when exactly one candidate survives. NOT type-aware: use names that appear literally (e.g. a template argument's class name), not guessed signatures.","type":"string"},"function":{"description":"Target function qualified name. Provide 'function' or 'usr' (usr wins when both are present).","type":"string"},"site":{"description":"Call site 'file:line:col' (as spelled in the compile command). Resolves the identity to the exact overload/template instantiation called at that site \u2014 the most precise disambiguator when you are looking at a specific call.","type":"string"},"usr":{"description":"Exact USR of the target function. Bypasses name resolution \u2014 use it to pick one overload/specialization when the name is ambiguous.","type":"string"}},"type":"object"},"name":"query_nearest_catches","needs":["graph","control_flow"],"records":"catches","verb":"query-nearest-catches"},{"cli":true,"description":"For each entry point, enumerate call paths reaching `function` via reverse-walking the call graph, and report Lock-kind RAII locals live on any edge of each path. Result: {paths:[{entryPoint, path:[fn...], locksHeld:[{typeName, varName, heldAt}]}], truncated, pathCount}. Truncated at 512 paths total. Walks only through edges with stable callee identity (indirect/function-pointer targets are skipped). An ambiguous name returns {ambiguous:true, candidates:[...]} \u2014 re-query with 'usr'.","inputSchema":{"properties":{"entry_points":{"description":"Entry points to root the reverse walk (default: configured entry points)","items":{"type":"string"},"type":"array"},"filter":{"description":"Literal substring matched against candidate usr, qualified name, and file to narrow an ambiguous name; resolves when exactly one candidate survives. NOT type-aware: use names that appear literally (e.g. a template argument's class name), not guessed signatures.","type":"string"},"function":{"description":"Qualified name of the target function. Provide 'function' or 'usr' (usr wins when both are present).","type":"string"},"max_depth":{"description":"Maximum number of frames above the target to walk (default: 20)","type":"integer"},"max_fan_in":{"description":"Skip expanding functions with more stored callers than this; skipped hubs are listed in the response. 0 disables (default: 1000).","type":"integer"},"site":{"description":"Call site 'file:line:col' (as spelled in the compile command). Resolves the identity to the exact overload/template instantiation called at that site \u2014 the most precise disambiguator when you are looking at a specific call.","type":"string"},"usr":{"description":"Exact USR of the target function. Bypasses name resolution \u2014 use it to pick one overload/specialization when the name is ambiguous.","type":"string"}},"type":"object"},"name":"query_locks_held","needs":["graph","control_flow"],"records":"paths","verb":"query-locks-held"},{"cli":true,"description":"Compute the intersection of locks held across paths reaching fn_a and fn_b. Lock identity is the tuple (typeName, varName); the same physical mutex under different variable names will not match. Result: {sharedLocks:[{typeName, varName, pathsA, pathsB}], shared, aOnly, bOnly, truncated}. An ambiguous name returns {ambiguous:true, candidates:[...]} \u2014 re-query with the *_usr parameter.","inputSchema":{"properties":{"entry_points":{"description":"Entry points to root the reverse walk (default: configured entry points)","items":{"type":"string"},"type":"array"},"fn_a":{"description":"First function qualified name. Provide 'fn_a' or 'fn_a_usr' (the usr wins when both are present).","type":"string"},"fn_a_filter":{"description":"Literal substring matched against candidate usr, qualified name, and file to narrow an ambiguous name; resolves when exactly one candidate survives. NOT type-aware: use names that appear literally (e.g. a template argument's class name), not guessed signatures.","type":"string"},"fn_a_site":{"description":"Call site 'file:line:col' (as spelled in the compile command). Resolves the identity to the exact overload/template instantiation called at that site \u2014 the most precise disambiguator when you are looking at a specific call.","type":"string"},"fn_a_usr":{"description":"Exact USR of the first function. Bypasses name resolution for 'fn_a' when the name is ambiguous.","type":"string"},"fn_b":{"description":"Second function qualified name. Provide 'fn_b' or 'fn_b_usr' (the usr wins when both are present).","type":"string"},"fn_b_filter":{"description":"Literal substring matched against candidate usr, qualified name, and file to narrow an ambiguous name; resolves when exactly one candidate survives. NOT type-aware: use names that appear literally (e.g. a template argument's class name), not guessed signatures.","type":"string"},"fn_b_site":{"description":"Call site 'file:line:col' (as spelled in the compile command). Resolves the identity to the exact overload/template instantiation called at that site \u2014 the most precise disambiguator when you are looking at a specific call.","type":"string"},"fn_b_usr":{"description":"Exact USR of the second function. Bypasses name resolution for 'fn_b' when the name is ambiguous.","type":"string"},"max_depth":{"description":"Maximum number of frames above each target to walk (default: 20)","type":"integer"},"max_fan_in":{"description":"Skip expanding functions with more stored callers than this; skipped hubs are listed in the response. 0 disables (default: 1000).","type":"integer"}},"type":"object"},"name":"query_same_lock","needs":["graph","control_flow"],"records":"sharedLocks","verb":"query-same-lock"},{"cli":true,"description":"Run dead code analysis via call graph reachability. Reports dead, optimistically-alive, and alive functions from the configured entry points. System-header functions are excluded by default; use include_system to include them.","inputSchema":{"properties":{"entry_points":{"description":"Entry point function names (default: configured entry points). Entry points are reachability SEEDS: a name shared by several functions (overloads) seeds ALL of them (deliberate union semantics \u2014 no disambiguation round-trip here).","items":{"type":"string"},"type":"array"},"file_prefix":{"description":"Only include dead functions whose source file path starts with this prefix.","type":"string"},"include_optimistic":{"description":"Include optimistically-alive functions (default: true)","type":"boolean"},"include_system":{"description":"Include functions whose source file lives in a system include directory such as /usr/include or compiler-internal clang/gcc include dirs (default: false). Stdlib template instantiations otherwise dominate the response.","type":"boolean"},"limit":{"description":"Maximum number of dead entries to return after filtering (default: 500).","type":"integer"},"name_prefix":{"description":"Only include dead functions whose qualified name starts with this prefix.","type":"string"},"offset":{"description":"Number of filtered dead entries to skip before returning (default: 0). Use with limit to paginate.","type":"integer"}},"type":"object"},"name":"analyze_dead_code","needs":["graph"],"records":"dead","verb":"analyze-dead-code"},{"cli":true,"description":"List every tracked channel (queue/map/event-bus instance registered via --channel-types-json) with its type, category, and producer/consumer site counts. Empty (not an error) when the server was started without --channel-types-json. Use query_channel on a channelId for the full site list.","inputSchema":{"properties":{},"type":"object"},"name":"list_channels","needs":["graph","channels"],"records":"channels","verb":"list-channels"},{"cli":true,"description":"List every producer and consumer call site on one channel: {producers:[...], consumers:[...]}, each site {channelType, category, operation, function, functionUsr, callSite, guards:[{conditionText, location, inTrueBranch, isAssertion}]}. A channel with multiple producers/consumers lists all of them \u2014 there is no single caller/callee edge to name.","inputSchema":{"properties":{"channel_id":{"description":"Channel identity from list_channels or a channelId in another channel tool's response.","type":"string"}},"required":["channel_id"],"type":"object"},"name":"query_channel","needs":["graph","channels"],"records":"","verb":"query-channel"},{"cli":true,"description":"List the channel sites (producer or consumer) inside one function. Use this when you already know a function pushes/pops on some tracked channel but not which channel or where its counterpart is.","inputSchema":{"properties":{"function":{"description":"Qualified function name or USR whose channel producer/consumer call sites to list.","type":"string"}},"required":["function"],"type":"object"},"name":"query_channels_for_function","needs":["graph","channels"],"records":"sites","verb":"query-channels-for-function"},{"cli":true,"description":"Compare two channel call sites for the evidence needed to explain an ordering assumption between them: whether they're the same channel, each site's enclosing guard conditions (e.g. only reachable when some flag is true), and which ThreadEntry/AsyncTask/PackagedTask/Invoke boundaries reach each site's enclosing function. Returns structural evidence for a human/LLM to build the explanation from \u2014 an empty concurrencyBoundaries list is absence of evidence, not proof of synchronous execution.","inputSchema":{"properties":{"call_site_a":{"description":"First channel call site, 'file:line:col' (from query_channel or query_channels_for_function).","type":"string"},"call_site_b":{"description":"Second channel call site.","type":"string"}},"required":["call_site_a","call_site_b"],"type":"object"},"name":"explain_ordering","needs":["graph","channels"],"records":"","verb":"explain-ordering"},{"cli":false,"description":"Re-index a single translation unit after source changes. Removes stale edges/contexts and re-runs all three analysis phases for the given file. Returns counts of edges and contexts removed and current totals.","inputSchema":{"properties":{"file":{"description":"Absolute path of the TU to re-index","type":"string"}},"required":["file"],"type":"object"},"name":"reindex_tu","needs":["graph","control_flow","channels"],"records":"","verb":"reindex-tu"}]}
